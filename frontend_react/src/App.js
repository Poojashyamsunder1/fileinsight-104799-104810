import React from "react";
import "./App.css";
import LoginPage from "./LoginPage";

/**
 * PUBLIC_INTERFACE
 * App (root component)
 * For login page showcase, renders just the LoginPage component centered.
 */
function App() {
  return <LoginPage />;
}

export default App;
